function initialize(){
    writeCount = 0;
}

function newIteration(){
    for (i=0; i<1000; i++){
        writeCount = writeCount + 1;
        String(writeLine);
        newLine = writeLine
        write_box.innerHTML = newLine;
    }
}